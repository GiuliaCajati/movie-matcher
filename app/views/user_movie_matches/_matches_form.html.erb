
<%= form_for @usermoviematch do |f| %>
<%= f.label :title, "Movie:" %>
<%= f.collection_check_boxes( :movie_id, Movie.all, :id, :title, {}, { multiple: true, size: 10 })%>
<br>
<br>
<%= f.submit button_text %>
<% end %>